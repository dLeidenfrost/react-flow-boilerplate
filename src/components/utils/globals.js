/* @flow */
// Middleware url

// Paths, type { key: string, value: string }
export const PATHS: { [string]: string } = {
  LOGIN: '/login',
  HOME: '/home',
  THEME: '/theme',
  RECOVER: '/recover',
};
